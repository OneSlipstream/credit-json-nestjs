export interface DetailsArrangementXML {
  arrstartdate?: string;
  arrenddate?: string;
}

export interface DetailsArrangement {
  // The date when the payment agreement started
  startedAt?: Date;
  // The date when the payment agreement ended
  endedAt?: Date;
}